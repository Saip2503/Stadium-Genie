{
  "name": "stadium-genie",
  "private": true,
  "scripts": {
    "vercel-build": "node scripts/write_env_for_vercel.js && if [ ! -d flutter ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && export PATH=\"$PATH:$(pwd)/flutter/bin\" && flutter config --enable-web && flutter pub get && flutter build web --release --base-href /"
  }
}
